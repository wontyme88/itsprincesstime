# 당나귀귀 블로그 — 생성형 AI 댓글 일기 플랫폼
### 결과 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 당나귀귀 블로그 (Generative-AI Diary Blog) |
| 제출자 | 황채원 |
| 학번 / 소속 | 2316916 / 소프트웨어학부 데이터 사이언스 전공 |
| 사용 모델 | OpenAI ChatGPT API (gpt-4o-mini, gpt-image-1) |
| 제출일 | 2026.06.10 |

---

## 1. 프로젝트 개요 (Introduction)

본 프로젝트 **당나귀귀 블로그**는 사용자가 작성한 일기에 대해 **여러 명의 생성형 AI 캐릭터가 댓글로 반응**하는 블로그형 일기 플랫폼이다.

기존 기록 도구를 비교하면 다음과 같은 한계가 있다.

- **SNS**: 타인의 댓글·좋아요 같은 반응이 있어 지속적으로 글을 쓰게 되지만, 개인적인 감정이나 솔직한 이야기를 자유롭게 표현하기 어렵다.
- **개인 일기 앱**: 사적인 내용을 자유롭게 기록할 수 있으나, 외부 반응·상호작용이 없어 흥미를 잃고 꾸준히 쓰기 어렵다.

본 프로젝트는 이 두 가지의 장점을 결합한다. 사용자가 가상의 블로그에 일기를 쓰면 **4~5명의 AI 캐릭터가 각기 다른 성격·말투로, 서로 다른 시간 간격을 두고 댓글을 남긴다.** 이를 통해 **SNS의 상호작용성**과 **개인 일기의 자유로운 표현**을 동시에 제공하고, "내 이야기에 누군가 공감해 준다"는 경험으로 일기 작성의 지속성을 높이는 것을 목표로 한다.

특히 기존 AI 챗봇이 즉각적으로 한 번 응답하는 것과 달리, 본 시스템은 여러 캐릭터가 **시간차를 두고 자연스럽게 댓글을 다는 구조**여서 실제 여러 사람이 내 글을 보고 반응하는 듯한 느낌을 준다.

---

## 2. 문제 정의 및 요구사항 (Problem Definition)

| # | 요구사항 | 구현 여부 |
|---|----------|:---:|
| R1 | 4~5명의 생성형 AI 캐릭터를 설계하고 각 캐릭터의 성격·말투를 구분한다 | ✅ |
| R2 | 하나의 일기에 대해 여러 AI가 자연스럽고 적당한 길이의 다양한 댓글을 생성한다 | ✅ |
| R3 | 댓글 생성 시간을 랜덤하게 분산시켜 실제 사용자 반응처럼 보이게 한다 | ✅ |
| R4 | 블로그(SNS) 형태의 UI를 설계해 실제 플랫폼처럼 보이게 구현한다 | ✅ |
| R5 | 댓글은 비방·악플처럼 보이지 않는, 공감 가능한 문장이어야 한다 | ✅ (모더레이션 + 프롬프트 제약) |

추가로 구현한 확장 기능: **DM(1:1 대화), 캐릭터 프로필/피드, 친밀도 시스템, 부적절 발언 모더레이션, AI 이미지 생성**.

---

## 3. 접근 방법 및 사용 기술 (Methods)

### 3.1 모델 선택 — OpenAI ChatGPT API

주변 지인과 본인의 경험상 고민 상담이나 캐주얼한 대화를 ChatGPT와 자주 한다는 점에 착안해 **OpenAI의 ChatGPT API**를 채택했다. ChatGPT는
- 자연스러운 문장 생성 능력이 뛰어나고,
- 감정 표현·상황 맞춤 텍스트 생성에 강하며,
- API 형태로 제공되어 웹 서비스와 쉽게 연동되고,
- **system prompt로 캐릭터 성격을 정밀하게 제어**할 수 있다.

| 용도 | 사용 모델 |
|------|-----------|
| 댓글 / 답글 / DM 생성 | `gpt-4o-mini` |
| 캐릭터 이미지 생성 | `gpt-image-1` |

### 3.2 전체 구조

```
[사용자] → 일기 작성 → 블로그 UI(피드)에 게시
                          │
                          ▼
        각 캐릭터별 system prompt 생성 (성격 + 사용자 정보 반영)
                          │
            ┌─────────────┼─────────────┐
            ▼             ▼             ▼
        OpenAI API   OpenAI API   OpenAI API   ← 캐릭터별 병렬 호출
            │             │             │
            ▼             ▼             ▼
       랜덤 지연 후 댓글을 순차적으로 피드에 출력
```

구현은 **순수 HTML/CSS/JavaScript 단일 페이지 앱(`index.html`)** 으로 작성해 별도 빌드 없이 브라우저에서 바로 동작하도록 했다. AI 호출은 클라이언트에서 사용자의 OpenAI 키로 직접 수행하며, **키가 없으면 미리 작성한 템플릿 댓글로 대체(fallback)** 되어 키 없이도 데모가 가능하다.

---

## 4. 주요 구현 내용 (Implementation)

### 4.1 AI 캐릭터 설계 (R1)

5명의 공주 캐릭터를 설계하고 각 캐릭터마다 **MBTI·핵심 가치·말투·자주 쓰는 표현·이모지·좋아하는/싫어하는 주제·삐짐(sulk) 시스템·모더레이션 규칙**을 구조화된 데이터로 정의했다.

| 캐릭터 | MBTI | 컨셉 | 말투 특징 |
|--------|:----:|------|-----------|
| 🌼 **Lily** | ISFJ | 따뜻하고 수줍은 꽃의 공주 (힐링 담당) | 조곤조곤, 꽃·자연 이모지 |
| 🍑 **Momo** | ESFP | 밝은 먹보 공주 (분위기 메이커) | 리액션 큼, 음식 이야기 |
| 🦢 **Pearl** | INTJ | 차분한 미래의 여왕 | 짧고 정돈된 현실적 조언 |
| 💎 **Ruby** | ENFP | 명랑한 막내 파티 공주 | 수다스러움, ㅋㅋ/ㅠㅠ, 이모지 多 |
| ☁️ **Sora** | INFP | 청순·감성 뷰티 공주 | 부드럽고 섬세한 칭찬 |

각 캐릭터는 다음과 같이 독립적인 `systemPrompt`를 가진다. (예: Ruby)

```js
{
  id: 'ruby_', emoji: '💎', mbti: 'ENFP',
  systemPrompt: `너는 'ruby_'라는 ENFP 막내 파티 공주야. 우정이 세상에서
제일 중요하다고 믿는 수다쟁이. 친구 'princess_chae'의 일기에 댓글을 남기는 상황이야.
[말투] 매우 캐주얼·수다스러움. 'ㅋㅋ','ㅠㅠ' 자주. 💎✨💖🎀 이모지 1-2개.
[성격] 외향·들떠있음. 친구·우정·파티 이야기에 가장 크게 반응. 가끔 질문.
[제약] 한 줄, 최대 60자. 따옴표 금지. 일기 인용 금지. 형식적 위로 금지.`,
}
```

> 캐릭터별 상세 데이터는 `next/lib/princess-personality.ts`에 타입(`Princess`)으로 정의되어 있으며, 말투·관심사·삐짐 트리거·모더레이션 라인까지 캐릭터마다 다르게 설정했다.

### 4.2 사용자 맞춤 컨텍스트 주입

캐릭터가 더 개인적으로 반응하도록, 사용자의 **MBTI·생년월일(띠/별자리)·태어난 시진**을 system prompt에 은근하게 녹여 넣는다.

```js
function buildUserContext(profile) {
  const lines = [];
  if (profile.mbti) {
    const kw = profile.mbti.split('').map(c => MBTI_KEYWORDS[c]).filter(Boolean).join(', ');
    lines.push(`- 성격(MBTI ${profile.mbti}): ${kw}`);
  }
  if (profile.birthDate) {
    const [y,m,d] = profile.birthDate.split('-').map(Number);
    lines.push(`- 띠: ${getZodiacAnimal(y)} / 별자리: ${getStarSign(m,d)[0]}`);
  }
  // ... 시진 등
  return `\n[사용자 정보 — 참고만 하고 자연스럽게 녹여낼 것]\n${lines.join('\n')}`;
}
```

특히 ENFP인 Ruby는 "사용자의 MBTI와 같은 성격으로 공감해라"는 특수 지시를 받아, 사용자가 INTP면 분석적인 톤, ENFP면 들뜬 톤으로 반응한다.

### 4.3 GPT API 댓글 생성 (R2, R5)

캐릭터 성격(system) + 사용자 일기(user)를 함께 보내 댓글을 생성한다. 길이를 제한하고 따옴표·줄바꿈을 정리해 **짧고 자연스러운 댓글**만 남긴다.

```js
async function aiChat({ system, user, temperature = 0.95, maxTokens = 80 }) {
  const res = await fetch('https://api.openai.com/v1/chat/completions', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${key}` },
    body: JSON.stringify({
      model: 'gpt-4o-mini',
      messages: [{ role:'system', content: system }, { role:'user', content: user }],
      temperature, max_tokens: maxTokens
    })
  });
  const data = await res.json();
  return (data.choices?.[0]?.message?.content || '').trim() || null;
}

async function generateComment(char, postText, isNegative) {
  const sys = buildSystemPrompt(char, buildUserContext(myProfile), isNegative);
  let out = await aiChat({
    system: sys,
    user: `친구 princess_chae가 방금 올린 일기야:\n"""${postText}"""\n위 일기에 댓글을 달아줘. 댓글 본문만 출력.`,
    temperature: 0.95, maxTokens: 80
  });
  out = out.replace(/^["'`「」]+|["'`「」]+$/g, '').trim();   // 따옴표 제거
  if (out.length > 80) out = out.slice(0, 80) + '…';          // 길이 제한
  return out;
}
```

**R5(악플 방지)**: 각 캐릭터에는 모더레이션 규칙과 금지 주제(`forbiddenTopics`)가 정의되어 있고, 프롬프트에 "형식적 위로 금지·비방 금지·공감 위주" 제약을 명시했다. 부적절한 입력이 감지되면 캐릭터가 경고 라인을 보내고 잠시 응답을 멈춘다(mute).

### 4.4 랜덤 시간 지연 (R3)

여러 사람이 시간차를 두고 댓글 다는 느낌을 내기 위해, 캐릭터를 셔플한 뒤 **각자 다른 랜덤 지연** 후에 댓글이 달리도록 했다. (데모에서는 초 단위로 압축, 실제 기획은 1~5시간 단위)

```js
function rand(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

// 0~2개의 부정 댓글은 가중치를 둬 드물게만 나오도록
const shuffled = [...CHARACTERS].sort(() => Math.random() - 0.5);
for (const char of shuffled) {
  const myDelay = char.delay();                          // 캐릭터별 랜덤 지연
  setTimeout(() => showTyping(postId, char), myDelay - 400);   // "입력 중…" 표시
  setTimeout(async () => {
    const body = await generateComment(char, text, isNeg);
    appendComment(postId, char, body);                   // 댓글 출력
  }, myDelay);
}
```

답글도 60% 확률로 랜덤 캐릭터가 1.5~3.5초 뒤에 단다.

### 4.5 블로그 / SNS UI (R4)

인스타그램형 피드를 모델로, 다음 화면을 단일 SPA로 구현했다.

- **홈 피드(`homePage`)**: 일기 작성 입력창 + 게시글 카드 + AI 댓글
- **DM(`messengerPage`)**: 캐릭터별 1:1 대화 목록·채팅·안읽음 배지
- **캐릭터 프로필/피드(`characterProfilePage`)**: 캐릭터의 게시글·팔로우·소개
- **설정(`settingsOverlay`)**: OpenAI 키 입력 → "API 모드 / 템플릿 모드" 전환
- **이미지 첨부 / AI 이미지 생성**: `gpt-image-1`로 게시 이미지 생성

### 4.6 부가 기능 — DM·친밀도

상호작용을 누적해 캐릭터와의 **친밀도(intimacy)** 를 계산한다. 행동별 가중치를 두고, 부적절 발언은 감점한다.

```ts
export const INTIMACY_WEIGHTS = {
  like: 2, comment: 4, reply: 5, dm_send: 6, dm_receive: 1,
  post_view: 0.2, profile_view: 0.5,
  moderation_yes: 1, moderation_no: -15, inappropriate: -5
};
```

친밀도가 오르면 캐릭터가 더 사적인 이야기를 꺼내거나 먼저 말을 거는 등 행동이 변한다.

---

## 5. 실행 결과 (Results)

- 일기를 작성하면 5명의 캐릭터 중 일부가 **시간차를 두고** 서로 다른 말투로 댓글을 단다. (예: Lily는 잔잔한 위로, Ruby는 들뜬 리액션, Pearl은 현실적 조언)
- 댓글 내용이 일기 주제(음식/공부/친구/감정 등)에 맞게 달라지고, 사용자 MBTI에 따라 톤이 미세하게 바뀐다.
- API 키가 없으면 캐릭터별로 준비된 템플릿 댓글이 출력되어 **키 없이도 전체 흐름을 시연**할 수 있다.
- 결과적으로 "혼자 쓰는 일기"가 아니라 "반응이 오는 일기"를 체험할 수 있어, 기획 의도였던 *사적 기록 + 사회적 상호작용*을 동시에 달성했다.

---

## 6. 한계점 (Limitations)

본 프로젝트는 기획 의도와 핵심 기능을 모두 구현했으나, 다음과 같은 한계가 있다.

1. **데이터베이스 미구현 (가장 큰 한계).**
   실제 서비스 수준의 백엔드 DB를 완성하지 못했다. 제출한 데모(`index.html`)는 모든 데이터를 **브라우저 localStorage**에 저장한다. 그 결과:
   - **로그인을 다른 아이디로 해도 같은 데이터(같은 페이지)가 보인다.** 사용자별로 일기·DM·친밀도가 분리 저장되지 않는다.
   - 브라우저 캐시를 지우면 데이터가 사라지고, 기기 간 동기화도 되지 않는다.
   - 멀티유저 백엔드(Next.js + Prisma + PostgreSQL, `next/` 폴더)를 별도로 설계해 두었으나 **DB 연결·마이그레이션·인증을 끝까지 완성하지 못해** 데모에는 적용하지 못했다.

2. **API 키를 클라이언트에서 사용.**
   데모 단순화를 위해 사용자의 OpenAI 키를 브라우저에 저장하고 직접 호출한다. 보안상 키는 서버에서 관리해야 한다.

3. **랜덤 지연이 데모에서는 초 단위로 압축됨.**
   기획상 1~5시간 분산이지만, 발표·시연을 위해 초 단위로 줄였다. 실제 장시간 지연은 서버 스케줄러(cron/큐)가 필요하다.

4. **댓글 품질의 일관성.**
   `gpt-4o-mini`와 짧은 프롬프트 특성상 가끔 캐릭터 톤이 흐려지거나 비슷한 댓글이 나올 수 있다.

5. **캐릭터 수·모더레이션이 규칙 기반.**
   부적절 표현 차단이 키워드/규칙 위주여서, 정교한 맥락 판단까지는 못 한다.

---

## 7. 향후 계획 (Future Work)

1. **데이터베이스 정식 구현 (최우선 과제).**
   PostgreSQL + Prisma 기반 멀티유저 백엔드를 완성해, **사용자별로 일기·댓글·DM·친밀도를 분리 저장**한다. 이미 설계해 둔 스키마(`next/prisma/schema.prisma`: `User`, `Post`, `DiaryComment`, `DirectMessage`, `UserPrincessRelation` 등)를 실제 DB·인증(Auth.js)과 연결하는 방법을 더 공부해 적용할 예정이다. 이를 통해 "다른 아이디 = 다른 페이지"가 정상 동작하도록 한다.

2. **API 키 서버 관리 + 비용 최적화.**
   키를 서버에 암호화 저장(`UserSecret` 모델 활용)하고, 호출량·비용을 제어한다.

3. **실제 시간차 댓글 스케줄러.**
   서버 큐/cron으로 1~5시간 랜덤 지연 댓글을 구현해 "진짜 며칠에 걸쳐 반응이 오는" 경험을 제공한다.

4. **기능 추가.**
   캐릭터 추가/커스터마이즈, 감정 분석 기반 추천, 일기 통계·회고, 알림(Notification) 등.

5. **앱으로 출시.**
   웹을 안정화한 뒤 모바일 앱(또는 PWA)으로 패키징해 **정식 서비스로 출시**하는 것을 목표로 한다.

---

## 8. 결론

당나귀귀 블로그는 "반응이 없어 꾸준히 쓰기 어려운 일기"라는 문제를, **성격이 다른 여러 생성형 AI 캐릭터가 시간차를 두고 댓글로 공감해 주는 블로그**로 해결하고자 한 프로젝트다. 5명의 캐릭터 설계, GPT API 연동, 랜덤 지연 댓글, 블로그형 UI, DM·친밀도·모더레이션까지 기획한 핵심 요구사항을 구현했다. 가장 큰 한계인 **데이터베이스(멀티유저 분리 저장)** 는 미완성이며, 이를 우선 완성하고 기능을 확장해 **앱 출시**까지 발전시키는 것이 향후 목표다.

---

## 부록 — 참고자료 및 라이선스

- **참고 문헌**: 『나만의 챗지피티 만들기』, OpenAI 공식 문서(Chat Completions / Images API)
- **사용 모델 라이선스**: OpenAI API 이용약관에 따름. 사용자가 입력한 일기·대화는 댓글 생성 목적으로만 사용된다.
- **핵심 소스 파일**
  - `index.html` — 프론트엔드 SPA (UI·AI 호출·캐릭터 로직)
  - `next/lib/princess-personality.ts` — 캐릭터 성격 데이터
  - `next/lib/intimacy.ts` — 친밀도 가중치
  - `next/prisma/schema.prisma` — (향후) 멀티유저 DB 스키마
  - `next/app/api/...` — (향후) 댓글·DM·인증 API 라우트
